Working workbooks removed from publication, restoring the original decision (July 16, 2026, approved): MERGED_3-8-2026.xlsx and Census_Merge.xlsx had been re-added to the repository during repo reorganization, reversing the June 15, 2026 decision below without noticing the conflict. Re-confirmed the original reasoning still holds: both workbooks contain pivot tables computed on full-year, not matched nine-month, percentages (e.g., -73.9% year-over-year for Los Angeles in Analysis4_Geographic), which would read as contradicting this project's 82% headline without context. Removed both files from Excel/, updated README.md and methods.html's repository descriptions to list only PROJECT_SUMMARY.xlsx, and added a closing note to DATA_QUALITY_NOTES.md Issue 7 confirming Census_Merge.xlsx is no longer part of the published repository. Separately, converted the two remaining ACS tables (Table_1_Median_Household_Income, Table_2_Race_Or_Ethnicity) from xlsx to csv and added both to data/acs/, removing the manual one-time export step REPRODUCE.md previously required; the xlsx originals remain as the version of record. Verified: replicate_r2.R's expected input paths were already csv-based and required no code change; row counts and headers on the new CSVs confirmed against the script's expected fields.

Equity table title restated as a finding (June 13, 2026, approved): "Complete Equity Analysis: All Variables Tested" became "All Six Variables Tested. None Predicted the Decline." States the result rather than describing the table's contents, matching the finding-style headers used elsewhere on the page, and replaces "Complete/All Variables" (which overpromised exhaustiveness) with the accurate count of six (income, rent burden, and four race/ethnicity groups). Verified: page parses clean.

Equity intro reworded to variable frame (June 13, 2026, approved): on the Equity page, "fell harder on lower-income neighborhoods, rent-burdened households, or communities of color" became "fell harder based on neighborhood income, rent burden, or racial and ethnic composition." Makes all three test variables parallel (not two neighborhood traits and one identity group), matches the "what we actually found" card language, and fits the finding: the page reports that composition did not predict the decline, so the variable frame is more accurate than naming a group as an affected class. The later sentence naming the four groups at equal rates is unchanged. Verified: page parses clean.

Medium/Low review fixes \u2014 inactive rate, RHHP dates, SCEP citation (June 13, 2026, approved): three corrections from the editorial review. (1) Inactive rate on Limitations changed from 16.0% (full file, 48,458) to 16.1% (analysis window, 7,765 of 48,085), matching the page's stated scope. Confirmed the nine-month window is used only for 2023-vs-2025 comparisons (the decline, the rate and intensity trends, per-city and per-ZIP change, the validation); all descriptive stats use the full 48,085 window. (2) RHHP dates standardized across Start Here, The Decline, Equity, and Limitations: adopted April 23, 2024, effective October 23, 2024, inspections began November 2024. Every page now leads with or includes the operative October/November dates rather than April alone, since April is legislative passage with no effect on inspections. (3) SCEP "two or more units" re-cited from the SCEP brochure (which says only "multi-family") to the LAHD Inspections and Fees page, which states the two-or-more-units scope verbatim; the brochure still supports the four-year cycle and notice points elsewhere. Verified: all four pages parse clean.

Methods page finalized and nav links added (June 13, 2026, approved): added the Methods link to all eight pages — a pill between Equity and Limitations on the seven dashboards, and TOC row 07 on the index with Limitations renumbered to 08. Wrote the repository structure into README_SEED: data/county/ and data/acs/ for raw downloads (County and ACS kept separate), data/derived/ for built files (MERGED_3-8-2026, the inspections-violations join carrying the helper columns; Census_Merge, the ACS merged to inspection ZIPs), with the raw-vs-built rule and ACS provenance. Fixed a stale 37,126 facility count to 37,009 in the seed. Final audit of Methods: every figure reproduces against the raw files, HTML parses clean on all pages, links resolve, nav order Equity to Methods to Limitations throughout.

Methods page built and voiced (June 13, 2026, approved): new standalone methods.html. Documents sources, scope, the 62,631 to 37,026 violation reconciliation, derived fields with the full 35-neighborhood crosswalk, the decline and the equal-window rule, violation rate/intensity/severity, the equity spec (bivariate OLS, six ACS variables, n=254), the second-extract cross-check, the Glendale/Torrance sensitivity (82.45% to 81.02%), tools and repository, and a reproducibility table. Rewritten in plain first-person voice; title simplified to "Methods." Added a Fields-used column map (including the previously-missed SERVICE DESCRIPTION), the property-type derivation from PE DESCRIPTION, and ACS provenance (Census Reporter, three tables, March 2026). Corrected one unprovable line: the 24,877 unmatched violations had been described as predating January 1, 2023 — replaced with the provable statement (no matching inspection, so no date or property to attach them to).

Severity reframed to two classifications (June 13, 2026, approved): the severity card on The Decline showed three slices (92/5/3). Confirmed against the violations file and the County Housing Inspection Guide that "out of compliance" is a compliance status, not a severity tier — the guide defines only Critical and Non-Critical and never uses "severity" or "out of compliance" — and that it is 2.6% of the window (964 of 37,026), with most out-of-compliance records sitting in unmatched/complaint data outside this routine-only scope. Decision: keep out of compliance out of Part 1, defer to Part 2 with the complaint data. Card now shows two County classifications, 94% non-critical and 6% critical of 36,062 classified violations. Property Types updated 92% to 94%. No mention of out of compliance remains anywhere in the portfolio.

Editorial review and facility-count fix (June 13, 2026, approved): full publication review against the raw files; every number verified reproducible. Corrected the index Dataset Summary facility count from 37,126 (full-file) to 37,009 (in-window), matching Limitations and the stated scope. The index City-of-LA strip card was briefly changed to 56.5% then reverted to 57%, restoring the round-percentage display register settled on June 11; 56.5% stays in the dashboard2/dashboard5 prose next to its raw count. Verified: div balance, links resolve, no stale 37,126 remains.

Percentage-register audit and one revert (June 11, 2026, approved): the previous pass decimal-ized the index strip's City of LA card to 56.5% to match the prose body, but a full audit of every percentage on every page showed the portfolio's actual rule is purpose-correct as built: round percentages in prose and titles ("fell 82%", "rose from 32% to 42%"), and round percentages in display-style cards (severity 92/5/3, property types 92/8). The one inconsistency was the index strip itself, where my earlier edit had put a decimal alongside neighboring round-percent cards. Reverted: 56.5% returned to 57% on the index strip card to match its prose-register neighbors. The remaining decimals on display cards are the 2.23, 2.28, 2.60 avg-violations-per-inspection counts on The Decline, which are raw quantities (not percentages) where the decimal carries the actual measurement; correct as-is. Final state: zero mixed-register cards across all eight pages. The 56.5% figure stays in prose contexts on dashboard2 and dashboard5 where it appears as a measurement next to its raw count (27,191 of 48,085), which is the right register for those sentences. Both conventions are now used purposefully and consistently. Verified after revert: div balance, brace balance, zero dashes on all eight pages.

Positionality geography narrowed (June 15, 2026, approved): on the Equity page (dashboard5.html), "a nonprofit homeless services organization in the San Fernando Valley" changed to "a nonprofit homeless services organization serving Los Angeles County." Reason: the organization's property footprint spans North Hollywood, Los Angeles, and other county cities, making "Los Angeles County" accurate; and removing the sub-regional identifier eliminates any implication of organizational representation, consistent with the author's disclosure agreement. No other pages were affected. Verified: page parses clean.

Typography consistency audit (June 15, 2026, approved): full audit of all nine HTML pages against the design system (Source Serif 4 display, IBM Plex Sans body, IBM Plex Mono for code specs on methods, 15px base). Two base-size inconsistencies fixed: (1) index.html html/body was 16px, set to 15px to match the other eight pages; (2) dashboard5.html html/body carried a duplicate declaration that set 15px then overrode it with 16px on the same rule — removed the 16px override, leaving a clean 15px base. Font families confirmed consistent across all pages (no rogue Times New Roman, Arial, or off-system fonts). Shared heading classes confirmed aligned: .db-title 26px and .chart-title 22px identical wherever they appear. Verified: all pages parse clean.

White-card separation fix across portfolio (June 15, 2026, approved): large lone white cards were dissolving into the gray-100 page background because the card edge (0.5px border, no shadow) was too low-contrast against the near-toned page. Rendered every page and confirmed the flat-card pattern was portfolio-wide. Applied a restrained two-layer soft shadow (0 1px 3px rgba(16,24,40,0.08), 0 1px 2px rgba(16,24,40,0.06)) and bumped the border to 1px solid #D0D5DD on the main white-card classes: index .about-card, dashboard0 .section-card, dashboard1 .severity-card, dashboard2 .chart-card and .table-card, dashboard3 .card, dashboard4 .chart-card, dashboard5 .card, dashboard6 .card, methods .card. Small gray-50 stat cards and navy implication cards left unchanged (already separated by tone). Verified by re-render: cards now read as distinct objects on the page; layout intact; all pages parse clean.

AI disclosure rewritten for accurate attribution (June 15, 2026, approved): the index disclosure was reframed from a defensive "the analysis is the author's, AI did the code" line to a methodology note that (1) claims the author's actual work explicitly — building the merged datasets, deriving fields, Excel analysis, and visualization design decisions — (2) links to the Methods page, and (3) attributes the front-end build to Claude. Relabeled "AI assistance disclosure" to "Methodology note — AI assistance." On the Methods page (Section 10, Front end), corrected the prior "I used AI for layout and charts; the writing is mine" wording, which implied the author co-wrote the code, to "the front-end code was written by Claude (Anthropic) to my direction," and added the Tableau-to-HTML rationale (free-tier design/color limits drove the move to a custom build). Reason: accurate attribution is the shared principle across credible AI-disclosure conventions and is what holds up under hiring-manager or County scrutiny. Verified: both pages parse clean.

README finalized for publish (June 15, 2026, approved): (1) corrected the About-section AI attribution from "the analysis, findings, and writing are mine; I used AI for layout and charts" to "the dataset construction, analysis, findings, and visualization design decisions are mine; the front-end code was written by Claude (Anthropic) to my direction" — matching the corrected site disclosure. (2) Softened "(published on GitHub Pages)" to "(hosted on GitHub Pages)" since the site is not yet live. (3) Set repo name to la-county-housing-inspections-dph (DPH suffix distinguishes these Department of Public Health inspections from LAHD/SCEP housing-code inspections); live URL updated accordingly. File-to-page mapping verified against actual files: dashboard4=Equity, dashboard5=Limitations, dashboard6=Look Up a Property, methods.html=Methods — repo tree confirmed accurate.

README pages list fix (June 15, 2026, approved): the "The pages" section listed all eight content pages but omitted the index/landing page itself. Added "Home: the landing page with the headline findings and table of contents" as the first entry, matching the repo tree's description of index.html. List now covers all nine files.

Data folder assembled and derived-file decision finalized (June 15, 2026, approved): the six-sheet exploratory workbooks (MERGED_3-8-2026.xlsx, Census_Merge.xlsx) will NOT be published, because their pivots use full-year counts (e.g. -75% / -46% year-over-year, 34,617 base) that would read as a contradiction of the site's matched nine-month 82% headline, and the workbooks contain undocumented working sheets. Instead, built a single clean SUMMARY_Figures.xlsx in data/derived/: inspections by year, violation rate, total violations, and intensity, all computed on the matched January–September windows, formula-driven from raw counts, zero formula errors. Verified every figure ties to the site and Methods page (82.4% decline, rate 31.6%-41.7%, intensity 2.23/2.28/2.60). Placed the five raw files unchanged (md5-verified identical to source) in data/county/ (2 CSVs) and data/acs/ (3 ACS tables). Updated README repo-structure text and tree, and Methods step 10, to describe the summary file instead of the merged workbooks. Removed README_SEED.md from the publish set (private notes) and added .gitignore to keep it out.

Dashboard0 section titles + 1960s suffix (June 15, 2026, approved): the Start Here page has a "Jump to" menu (Regulatory History, Key Terms, Agencies), but only the Agencies section had a visible title; the other two dropped readers into body text with no heading. Added matching navy title-bar headers "Regulatory History" (section-3) and "Key Terms and Definitions" (section-4), using the same .section-header / .section-title markup as the existing Agencies bar. Chose plain titles with NO number circles: the sections are independent reference material reached via a jump menu in any order, not a step-by-step sequence, and the existing Agencies bar is unnumbered, so no-numbers keeps all three consistent. Separately, fixed the timeline date "1960s" so the trailing s renders at 0.72em lowercase, reading as a decade suffix rather than part of the number. Other pages audited: dashboards 1-5 already title their content cards with descriptive headlines, Methods has all 14 sections titled, dashboard6 is a tool page with labeled blocks; none needed changes. Verified live render and div balance.

Hiring-review quick fixes (June 15, 2026, approved): three fixes from a cold-screen review of the portfolio as a job artifact. (1) Index hero sentence reworded — the original "inspections that found violations found 17% more of them on average" was confusing; now reads "the share of inspections finding a violation rose from roughly 32% to 42%, and the average number of violations per inspection rose 17%." (2) Softened "32%" to "roughly 32%" since the precise figure is 31.6% (rounding up to 32 without qualification could read as imprecise on a rigor-focused portfolio; 41.7 to 42 is standard). (3) README About section reframed: the original described the QA work as "invisible: checking records, catching errors," which read as passive and hid the strongest analyst proof point; rewritten to lead with active systems-building ("I build the tracking systems that catch data errors before they affect funding") while keeping the personal voice and the upstream/downstream framing. Reason: the review flagged that the systems-building work is a stronger analyst signal than the portfolio itself and was being undersold.

1960s suffix size correction (June 15, 2026, approved): the earlier 0.72em suffix overcorrected and rendered the "s" so small it read like a subscript/typo. Changed to Option B — full-size, lowercase only (no size reduction). The original problem was the capital "S" reading as part of the number 1960; lowercase alone fixes it without shrinking. Now renders as a normal "1960s."

Disclosure wording smoothed (June 15, 2026, approved): in the index AI-disclosure sentence, changed the verb phrase "deriving the analytical fields" to "creating the fields the analysis needed" — plainer for a general/hiring-manager reader. Kept the technical noun "derived fields" intact on the Methods page, README, and dashboard5, where it is the standard data term and reads as precise rather than jargon. Distinction: keep the noun where a technical reviewer expects it, drop the verb where the audience is general.

Census Reporter provenance clarified (June 20, 2026, approved): Census Reporter's own about page states it is unaffiliated with the U.S. Census Bureau, which could read ambiguously out of context. Added one sentence to Methods step 1, immediately after the ACS citation, clarifying that Census Reporter is an independent non-profit that does not collect its own data, it provides an interface to the Bureau's published ACS tables, identified by the official table IDs already cited (B19013, B03002, B25070). README left unchanged since its one-line bullet already names the table IDs, which is the part that actually anchors the citation to the primary source; the fuller provenance note belongs in Methods, the authoritative documentation page.

Em dash cleanup on Methods (June 21, 2026, approved): Methods had accumulated 27 em dashes across 11 sentences, dense enough to read as cluttered rather than precise. Removed all of them. The 12-dash field glossary (ACTIVITY DATE, SERIAL NUMBER, etc.) became a TERM: definition colon format, the standard punctuation for a label/definition list and cleaner than repeated dashes. The remaining 10 sentence-level em dashes (the ACS table list, the RHHP field note, the helper-columns sentence, the 86%-correction story, the violation-rate sentence, the equity sample note, the R-squared threshold, the cross-extract validation sentences, and the repository pull-dates sentence) were each reworded with commas or a colon in place of the dash, preserving the exact meaning. En dashes in year ranges (2020-2024, etc.) were left untouched, those are a different, correct use. Verified zero em dashes remain, div balance intact (103/103), and the crosswalk glossary renders cleanly.

Two data notes folded into Limitations grid (June 21, 2026, approved): a separate navy-accented callout box ("Two data notes") sat directly below the navy-accented "About the Analyst" card, so the two looked like duplicate stacked components despite holding unrelated content, one personal, one a data caveat. Removed the standalone box and turned its two notes into limitation cards 15 ("Inspection Counts Are Not Property Counts") and 16 ("Inactive Program Status, Unexplained"), matching the existing .lim-card grid style used by cards 1-14. Also replaced the jargon term "Grain" (a database term for what one row represents) with plain language in both this card and the parallel note in Methods, where it's now labeled "One record, one inspection," matching the page's existing short-label convention ("One program," "Sample," "Result"). Net effect: one fewer distinct visual component on the page, content unchanged, fits the established "two categories" limitations framework instead of sitting outside it.

README fixes (June 26, 2026, approved): three updates. (1) Limitations count corrected from "fourteen" to "sixteen" in both the pages list and the Limitations section, reflecting the two additional cards (15 and 16) added when the data notes were folded into the grid. (2) README About section location updated from "in Los Angeles" to "serving Los Angeles County and the San Fernando Valley" to accurately reflect service area without disclosing employer name or specific location. (3) Confirmed dashboard6.html = Look Up a Property is correctly mapped in the repository tree; the apparent numbering discrepancy is a display-order choice in the nav, not a file naming error.

"Data case study" simplified to "case study" (June 26, 2026, approved): "data case study" is not a standard industry term in analytics or consulting; "case study" alone is understood to involve data in these contexts. Removed "data" prefix from the README opening line, the only place it appeared. All other "case study" references were already clean.

Dashboard2 sticky summary banner + clearer stat labels (June 26, 2026, approved): two changes to the Violations by Area page. (1) The city summary banner (the three numbers shown when a city is selected) is now sticky, pinning to the top of the viewport as the user scrolls the long city tables, so the selected city's numbers stay visible while clicking through rows far down the page. To prevent a collision between two pinned elements, the sticky behavior was removed from the table header rows (a minor nice-to-have) so only the banner pins. (2) The three stat labels were reworded for plain-language clarity: "Total Inspections" to "Housing inspections" (reminds the reader the subject is housing inspections specifically, not inspections in general); "Decline (Jan-Sep 2023 vs Jan-Sep 2025)" to "Decline in housing inspections, 2023 to 2025" with the 9-month-window methodology moved into a hover tooltip; "Combined Violation Rate (2023-2025)" to "Inspections finding a violation, 2023 to 2025" (states what the number actually counts; the raw-count pooling method moved to a hover tooltip). Both parent-city and child-neighborhood stat blocks updated identically. Note: sticky scroll behavior should be visually confirmed in a real browser before publishing, as the headless renderer did not reliably trigger the row-click handler.

Dashboard6 search helper scope line (June 26, 2026, approved): added one sentence to the Look Up a Property search helper text: "If an address is missing, it may not have been inspected between January 2023 and September 2025." Reason: a public user who searches a real building and finds nothing could wrongly conclude the data is incomplete or broken; the dataset's zero-result message already explains the Jan 2023 to Sep 2025 scope, but only after a search returns nothing. This surfaces the same scope reminder up front, by the search box, so users are primed before they search. Chose not to add a note about substring matching (e.g. "741 finds 1741") to keep the helper line short; that mechanic is rare, self-correcting, and better left to the curious than placed in front of every user.

Validation section made collapsible on The Decline (June 26, 2026, approved): the validation block ("A second download reproduces these numbers"), one of the portfolio's strongest rigor signals, is a long narrative plus a by-the-numbers table that interrupted the decline story's momentum and duplicated the full write-up already on Methods (step 8 plus the reproducibility table). Rather than remove or trim it (which risked hiding the strongest evidence or maintaining two copies), wrapped it in a native HTML details/summary element, collapsed by default. The summary shows the Validation eyebrow, title, a one-line hint, and a Show/Hide toggle; the full narrative and table expand on click. No JavaScript, fully accessible. Skeptical readers still see validation exists right next to the claim and can open it; readers who trust the finding scroll past. Methods remains the complete source of truth. Verified both collapsed and expanded states render correctly; div balance intact.

Removed all Part 2 references (June 26, 2026, approved): three mentions promised or mischaracterized a follow-up study. (1) dashboard1 validation footer "examined in Part 2" removed (committed to an unscheduled follow-up and framed it as just more data). (2) dashboard1 RHHP implication note: removed "Part 2 examines the RHHP data directly" but kept the load-bearing reasoning that RHHP (unincorporated only, under 1% of analyzed inspections) does not explain the decline, that confound check stands on its own. (3) methods.html: the 373 excluded Oct-Dec 2025 records were described as "held for Part 2"; reworded to "excluded to keep a clean nine-month comparison across years," the accurate reason. Rationale: Part 2 is now scoped as the RHHP comparison study (DIB mandated vs. non-mandated schedules), not a time-extension of this extract, and it is unscheduled; a published portfolio should not carry promises of unfulfilled follow-ups or mischaracterize what they are. References can be added back when Part 2 actually exists.

RHHP link fixed (June 27, 2026, approved): the "Rental Housing Habitability Program (RHHP) enacted" link in the dashboard1 "What this suggests" law timeline pointed to the https:// form of the LA County DPH RHHP page. The County's Public Health server serves this page over http and redirects https to http, so the https form could fail to load or show "not secure" in some browsers. Changed to http://www.publichealth.lacounty.gov/eh/about/rental-housing-habitability-program.htm, the canonical form the server actually serves, matching every other publichealth.lacounty.gov link in the portfolio (which were already http). Verified the page loads and confirms the timeline facts (April 23 2024 enactment, Chapters 8.53 and 8.55, unincorporated-area scope, every-4-years routine inspection cycle, effective October 2024, inspections from November 2024). The same destination was already correctly linked (http) on dashboard0's Key Terms RHHP card. No other https publichealth links remained. Recommend confirming the link click on the live page before public launch.

Validation hint line hidden when open (June 27, 2026, approved): the collapsible validation section's helper line ("How I confirmed the decline is real... Click to expand.") was still showing after the section was expanded, where it was redundant. Added a CSS rule so the hint displays only in the collapsed state and disappears when open. Collapsed state unchanged; expanded state now goes cleanly from title to content.

Dashboard2 banner change-stat bug fixed (June 27, 2026, approved): the city summary banner's middle stat had two bugs. (1) It hard-coded a minus sign and the word "Decline" onto every city, so cities where inspections ROSE between Jan-Sep 2023 and 2025 displayed an impossible double-minus value (e.g. La Crescenta "--192%") and were mislabeled as declines. Seven places rose and were affected: El Segundo, Avalon, La Crescenta, Montrose, Stevenson Ranch, and the Tujunga and Sunland child rows. (2) No small-sample guard, so tiny-base rises produced dramatic, misleading percentages (Stevenson Ranch 1->6 would read +500%). Replaced both inline calculations (parent and child blocks) with a single shared changeStatHtml() helper that: computes the signed change (+ for rises, - for falls); relabels the stat "Change in housing inspections, 2023 to 2025" so it is accurate in both directions; and applies the SAME sample-size thresholds the page already uses for violation rates: base under 10 suppresses the percentage and shows direction plus raw counts ("Rose ⚠ (6 → 24)"), base 10-49 shows the percentage with a ⚠ warning, base 50+ shows it clean. Declines remain bounded at 100% and unaffected. This keeps one documented suppression rule across the whole page rather than inventing a parallel one. Verified the logic on all affected cities and confirmed the live render (La Crescenta now shows "+192% ⚠", not "--192%").

Dashboard2 change-stat colors + under-50 suppression (June 27, 2026, approved): refined the banner change stat from the prior fix. (1) Added muted, palette-matched direction colors to the change stat only (not the violation rate, which would imply good/bad): rises show green (#3B6D11) with a + sign, falls show orange-red (#993C1D) with a - sign, suppressed small-sample cases show amber (#854F0B). (2) Raised the suppression threshold from base<10 to base<50, so any city with fewer than 50 inspections in the Jan-Sep 2023 baseline shows "Rose/Fell ⚠ (base → 2025)" with raw counts instead of a percentage. This means Avalon (32), La Crescenta (13), Sunland (10), Montrose (6), and Stevenson Ranch (1) all suppress; only solid 50+ samples (Glendale -99%, El Segundo +26%, Tujunga +93%) show a real percentage. Rationale: per-city banner numbers are exploratory drill-downs, not load-bearing claims, so unbounded small-sample rise percentages (e.g. Stevenson Ranch 1→6 = +500%) should never display as if they were findings. The under-50 line is stricter than the page's 10/10-49 rate threshold, applied specifically because rise percentages are unbounded (unlike declines, capped at 100%) and more prone to misleading magnitude. Verified all three states render live.

OPEN/NEXT: dashboard4 (Look Up by ZIP / equity) has the same class of small-sample issue. Four ZIPs show declines over 100% (impossible, so they are actually increases): 91020 (212.5%), 91040 (154.5%), 91214 (192.3%), 91381/Stevenson Ranch (500%). The dashboard4 code already labels direction correctly (Increase vs Decline) but its data does NOT carry a per-ZIP inspection count, so the same sample-size suppression cannot be applied without regenerating the dashboard4 ZIP dataset to include each ZIP's base count. Decide next session: regenerate dashboard4 data with per-ZIP counts and apply the same suppression (consistent), or hold. This was found during a broader integrity sweep; other checks (violation rates within 0-100, nine-month base never exceeding full year, no negative counts, child-sum differences explained by the existing not-shown-individually note) all passed.

Dashboard6 countywide violations card removed (June 28, 2026, approved): removed the "Most common violations countywide" card from the Look Up a Property page (dashboard6). Removed all four associated pieces: the HTML card block, the hardcoded RANKS data array, the rk/mx rendering variables, and the forEach loop that built the bar chart rows. Rationale: dashboard6 is a search tool and users arrive in lookup mode; a countywide top-10 violations chart is friction that delays the tool without serving the page's purpose. The countywide violation pattern is already described in prose on dashboard3 (Property Types), and dashboard3's "More Than 1 in 4 Violations Involve Damage to Walls, Ceilings, and Floors" card is the load-bearing finding there and was not moved or changed. No other pages affected.

Northridge consolidated into the City of Los Angeles grouping (July 5, 2026, approved): reversal of a settled decision. Northridge (495 records by city name) had been kept as its own top-level city on dashboard2 even though it is a City of LA neighborhood, a choice documented in DATA_QUALITY_NOTES Issue 3 and dashboard5 Limitation 6. This session confirmed from raw that the old parent total 27,191 was internally consistent all along (26 visible children summing to 27,117 plus 10 hidden neighborhoods summing to 74) and that Northridge was the only LA neighborhood genuinely outside the grouping. Northridge now enters as a visible child row keyed by city name (495; its rates 22.4/44.6/47.7, combined 34.3, reproduce exactly from raw). The parent becomes 27,686 (19,748 / 5,268 / 2,670 by year; Jan-Sep 2023 baseline 15,572), per-year violation rates 32.9/36.5/40.1, combined rate unchanged at 34.3. The City of LA share moves from 56.5% to 57.6%, and the June 11 and 13 register decision (57% round on the index strip, 56.5% in prose) is reversed in favor of 57.6% everywhere: index strip, dashboard2, dashboard5, methods, README. The grouped-label count moves from 35 to 36 (methods crosswalk updated), a new methods note explains why city-keyed (495) and ZIP-keyed (500) counts differ with Census ZCTA and HUD USPS crosswalk sources, and DATA_QUALITY_NOTES Issues 3 and 4 plus the summary table were brought current. A dated addendum in R2_SOURCE_OF_TRUTH records that this change is city-name-keyed and leaves the ZIP-keyed 254-sample regression untouched. Verified: parent equals sum of visible children plus hidden note (27,686 = 27,612 + 74); all Northridge figures and the new parent year totals and rates recomputed from the raw inspections and violations files; zero instances of 27,191 or 56.5% remain on any page.

Equity page language neutralized, suppression item closed, replication script added (July 5, 2026, approved): three related changes. (1) Two dashboard4 phrases claimed things the regression does not measure. The intro callout's "targeted disinvestment in historically underinvested communities" and the explainer's "indicating targeted inequity" were both rewritten to name what was actually tested: income, the share of renters stretched by housing costs, or the Hispanic, White, Black, or Asian share of a ZIP code. Items 3 and 4 from the same sweep ("No Demographic Disparities" title, "Equity" nav label) were reviewed and deliberately kept. The interpretive framing in the closing section is unchanged. (2) The open dashboard4 small-sample question from June 27 is closed as retained-as-published: the four ZIPs above +100% (91020, 91040, 91214, 91381) stay in the regression, disclosed in the dashboard4 footer, because suppressing them would change the locked R² values and require superseding the source-of-truth file. (3) The repository now carries REPRODUCE.md and analysis/replicate_r2.R, a base-R script using lm() that rebuilds the full chain from the raw files: 48,085 inspections, 289 ZIPs, the three exclusions in documented order (24, 6, 5), n = 254, and the six R² values. README's reproducibility section and repository tree were updated to point to it. Verified: the script was run against the raw files in this session and returned PASS on all six values (income 0.0155, rent burden 0.0040, Hispanic 0.0153, White 0.0203, Black 0.0196, Asian 0.0047) at n = 254; grep confirms zero instances of "disinvestment" or "inequity" remain on dashboard4.
